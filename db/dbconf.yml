development:
  driver: postgres
  open: $DATABASE_URL_DEV

staging:
  driver: postgres
  open: $DATABASE_URL_STAGING

production:
  driver: postgres
  open: $DATABASE_URL_PROD

data:
  driver: postgres
  open: dbname=gruff_data sslmode=disable

local:
  driver: postgres
  open: dbname=gruff sslmode=disable

#open: $DATABASE_URL
